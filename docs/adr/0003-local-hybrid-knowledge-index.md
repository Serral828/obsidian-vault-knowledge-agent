# Use a local hybrid knowledge index

The Knowledge Agent will retrieve Vault Evidence from a locally stored, incrementally updated index that combines full-text signals with Obsidian knowledge relations. Vector similarity is an optional enhancement rather than a prerequisite, so the product remains useful without an embedding model and never requires cloud indexing by default.
