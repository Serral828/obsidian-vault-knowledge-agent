# Discover models from configured services

The model picker will contain only models returned by the selected Model Connection's discovery endpoint. A manually entered model ID is allowed only when a service lacks model discovery and will be visibly marked as manual; the plugin will not ship guessed or hard-coded model names.
